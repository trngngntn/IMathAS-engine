{
    "require": {
        "php": ">=8.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^11"
    },
    "comment": "Composer is DEV-ONLY here (it installs PHPUnit). The engine has no runtime autoloader dependency — production loads classes via src/Engine/autoload.php. autoload-dev lets PHPUnit find the test classes.",
    "autoload-dev": {
        "psr-4": {
            "IMathAS\\Engine\\Tests\\": "tests/Engine/"
        }
    },
    "config": {
        "process-timeout": 0
    },
    "scripts": {
        "test": "phpunit"
    }
}
