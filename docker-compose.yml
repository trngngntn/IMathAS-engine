services:
  web:
    image: nginx:latest
    ports:
      - "8088:80"
    volumes:
      - ./:/var/www/html
      - ./default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
  php:
    build:
      context: ./
    volumes:
      - ./:/var/www/html
